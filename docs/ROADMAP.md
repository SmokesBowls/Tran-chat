## Phase 2
Real call foundation, not dummy-only validation.

- implement minimal signaling backend immediately
- add `POST /signal/offer`
- add `POST /signal/answer`
- add `POST /signal/ice`
- add `GET /signal/poll`
- test on real devices and real mobile/Wi-Fi conditions
- validate call stability before captions, chat overlay, or translation

Do not defer real signaling integration until the end of the project.
